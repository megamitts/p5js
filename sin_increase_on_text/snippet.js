let minVal = 1;
  let maxVal = 40;
  let calculatedValue = minVal + ((sin(t) + 1) / 2) * (maxVal - minVal);
  
  textSize(calculatedValue);

/* This snippet will lock the sin range to the minVal and maxVal


t is the angle increased usually by 0.02 to look smooth

*/
